# Audit — PR #11: API-validation hardening: 500s → 400 + money-endpoint HTTP tests

**Branch:** `claude/api-validation-hardening` → `main` (base `b6c5fdc`) · **Head SHA:** `e3f18b4` · **Auditor run:** 2026-06-11
**CI on the PR:** green on head sha and on main post-merge (verified by session owner; auditor has no GitHub access — worked from local git) · **Reviewer:** independent auditor (does not trust PR prose)

> **Protocol note (merge-before-audit):** PR #11 was **already merged** (merge commit `2d5cb6e`)
> before this audit ran. The Session Relay Protocol intends `/handoff-audit` to gate the open PR;
> auditing post-merge means a FAIL here would require a revert rather than a block. Flagged for
> owner awareness — the audit itself proceeded against the exact base...head range.
> Working-tree note: `src/growpodempire.egg-info/SOURCES.txt` carries an uncommitted local
> modification — it is a build artifact (regenerated by the editable install), not part of this PR.

## Diff inventory

`git diff --stat b6c5fdc...e3f18b4` → **7 files, +182/−12** — matches the PR claim exactly.

| File | +/− | Commit |
|------|-----|--------|
| `src/growpodempire/api/validation.py` | +17 | d5befdc |
| `src/growpodempire/api/game_api.py` | +31/−5 (net) | d5befdc |
| `src/growpodempire/services/game_service.py` | +10 | d5befdc |
| `tests/test_security.py` | +93 | d5befdc |
| `docs/HANDOFF.md` | +18/−7 (net) | d5befdc |
| `docs/memory/standups/2026-06-10-lut-report.md` | +14 | d5befdc |
| `.claude/settings.json` | +11 | **e3f18b4** (second commit) |

No `web/` files in the diff → web checks correctly skipped.

## Claims vs. evidence

| # | PR claims | Verified? | Evidence (`file:line`, current main == head) |
|---|-----------|-----------|------------------------|
| 1 | New `validation.number()` finite-float-in-range coercer rejecting NaN/inf/non-numeric | ✅ | `src/growpodempire/api/validation.py:48-62` — `float()` coercion with `(TypeError, ValueError)` → `GameError`; `if not (low <= n <= high)` rejects NaN (all NaN comparisons False) and ±inf (out of range). Exported/imported at `game_api.py:27`. |
| 2 | `set_environment` validates its 5 sensor params → 400; previously raw store → TypeError → 500 on next sim read | ✅ | New: `game_api.py:666-670` (`number()` on temperature/humidity/co2/light/ph with physical bounds), validated values passed at `:672-675`. Old path confirmed at base: route passed `data["temperature"]` etc. raw; `simulation_service.py:192-196` stores raw (`pod.temperature = temperature`); `simulation/engine.py:116` does `outside(env["temperature"], t_lo, t_hi)` — a string TypeErrors there on the *next read of every plant in the pod*. Regression tests: `tests/test_security.py:193` (`test_set_environment_rejects_non_numeric`), `:211` (`test_set_environment_rejects_out_of_range`). |
| 3 | `advisor/auto-care` validates `budget`/`max_actions` → 400, kept separate from the 404 plant-not-found mapping | ✅ | `game_api.py:567-581` — validation in its **own** try/except returning `_error(str(e))` (default 400) *before* the main try whose `except GameError` → 404 (confirmed at base: `return _error(str(e), 404)`). Base `autocare_service.py:156-159` did raw `float(budget)`/`int(max_actions)` → ValueError → 500, confirming the prior failure mode. Test: `tests/test_security.py:226` (`test_auto_care_rejects_non_numeric_budget`). |
| 4 | `create_player` strips + length-checks username, pre-checks email uniqueness → 400 (was 201 junk account / 500 IntegrityError) | ✅ | `services/game_service.py:99-107` — `(username or "").strip()`, `1 <= len <= 64` check, email strip/normalize, duplicate-email pre-check raising `GameError("Email already registered")`. Tests: `tests/test_security.py:237` (`test_create_player_rejects_blank_username`), `:242` (`test_create_player_rejects_duplicate_email`, asserts first POST 201, dup 400). |
| 5 | +8 HTTP-boundary tests: withdraw/deposit auth 401 / wrong-key + cross-player IDOR 403 / bad-amount 400, plus the validation-400 regressions | ✅ (⚠️ phrasing) | Exactly 8 new tests in `tests/test_security.py`: `:193,:211,:226,:237,:242` (5 validation tests covering the **4** regression areas — set_environment has two tests) + `:250` (`test_withdraw_requires_auth_and_blocks_idor`: no-key 401, wrong-key 403, A-key-on-B IDOR 403), `:266` (`test_withdraw_validates_amount`: −5 / "lots" / missing → 400), `:277` (`test_deposit_requires_auth_and_blocks_idor`: cross-player 403). All 8 re-run by name: **8 passed**. 189 (prior baton) + 8 = 197 ✓. Note: deposit gets the IDOR case only (no explicit no-key-401 / bad-amount tests for deposit) — minor asymmetry, not a false claim as worded in the commit. |
| 6 | 197 passed, coverage 79.3% → 80.03%; lint/check-memory/check-migrations green | ✅ (⚠️ 0.02pp) | Auditor re-ran all gates — see below. **197 passed, 80.01%** measured vs 80.03% claimed (0.02pp environment variance; gate ≥79% comfortably holds). Lint, check-memory, check-migrations all green. |
| 7 | "No behavior change to any happy path" | ⚠️ holds for happy paths, with edge-path caveats | True for well-formed input. Strictly-changed *edge* paths: (a) `max_actions > 100` was previously **clamped** (base `autocare_service.py:159` `min(int(max_actions), cap_actions)`) and is now a 400 (`game_api.py:577` `positive_int(..., maximum=100)`); `max_actions <= 0` was clamp-to-0 (no-op success), now 400. (b) Whitespace-padded usernames were previously stored verbatim (201), now stripped (possible new collision-400 with an existing trimmed name). (c) Out-of-range sensor values (e.g. temperature 200) were previously accepted, now 400. All are junk/edge inputs, arguably the point of the PR — but the claim is over-broad as written. |
| 8 | Second commit `e3f18b4` "read-only permission allowlist" | ✅ content, ⚠️ scope | Touched **only** `.claude/settings.json` (+11): `permissions.allow` for `Bash(make test|lint|check-memory|check-migrations)` + 3 read-only GitHub MCP tools (`pull_request_read`, `list_pull_requests`, `actions_list`). All entries are read-only/gate commands — no write permissions granted. **Scope-adjacent**: not part of the declared RISK #11/#8 scope; harmless dev-tooling, but it rode an unrelated PR. |

## Gates re-run by the auditor

- `make test` → **197 passed in 48.65s · coverage 80.01% ≥ 79.0% gate** ✅
- `make lint` → `ruff check --select=E9,F63,F7,F82 src tests` → **All checks passed!** ✅
- `make check-memory` → **Memory integrity OK — 18 files, links + ✅ citations resolve** ✅
- `make check-migrations` → **Alembic single-head OK — head: `f1a2b3c4d5e6`** ✅
- Targeted: the 8 new PR tests run by name → **8 passed** ✅
- web: **not touched** (no `web/` files in diff) → web checks correctly not required ✅

## Scope check

- **Declared scope:** RISK #11 (validation 500s) + RISK #8 (money-endpoint HTTP tests).
- **In-scope diff:** `api/validation.py`, `api/game_api.py`, `services/game_service.py`
  (validation only — 10 lines, no logic beyond input normalization), `tests/test_security.py`,
  `docs/HANDOFF.md` + standup addendum (baton upkeep — protocol-required).
- **Scope creep / out-of-scope changes:** `.claude/settings.json` (commit `e3f18b4`) —
  **scope-adjacent**, not in the declared scope. Content is a read-only permission allowlist
  (the four make gates + three read-only GitHub MCP tools); zero gameplay/runtime impact.
  Flagged for awareness, not severity.

## Carried-risks ledger check

- **Any OPEN RISK silently dropped?** **No.** All 11 rows survive in `docs/HANDOFF.md:82-92`.
  Movements: #8 OPEN→PARTIAL, #11 OPEN→PARTIAL — both with named remaining work.
- **Any risk marked FIXED without a test backing it?** **No.** RISK #11's
  "validation 500s→400 **FIXED** (test-backed)" (`docs/HANDOFF.md:92`) is backed in this very
  diff by: `test_set_environment_rejects_non_numeric`, `test_set_environment_rejects_out_of_range`,
  `test_auto_care_rejects_non_numeric_budget`, `test_create_player_rejects_blank_username`,
  `test_create_player_rejects_duplicate_email` (`tests/test_security.py:193,211,226,237,242`).
  #11's remainder (Redis rate-limit storage, `get_level` gating) is explicitly carried.
- ⚠️ **Minor ledger narrowing (RISK #8):** the row's old text read "money/**mint** endpoints 40%
  covered, no HTTP auth/IDOR/validation tests". The rewrite credits withdraw/deposit tests
  (correct) but the **mint** HTTP-boundary tests are neither added (no mint auth/IDOR test in
  `tests/test_security.py`; `tests/test_minting.py` is service-level) nor listed in the row's
  "Remaining" clause (`docs/HANDOFF.md:89` lists only vitest/Playwright, F2, F3). The mint
  HTTP blind spot is still real and has dropped out of the ledger text.

### Baton-integrity finding (protocol) — CONFIRMED

The baton was **not fully rewritten** by `/closeout`:
- Header (`docs/HANDOFF.md:7-11`) correctly says this chat = API-validation-hardening,
  "validation 500s→400 + money-endpoint HTTP tests (coverage 79.3%→80.0%, 197 tests)". ✅
- **`## What THIS chat did` (`docs/HANDOFF.md:51-61`) still describes the PREVIOUS chat's
  concurrency-core work** (wallet optimistic locking, migration `f1a2b3c4d5e6`, F5 flake fix,
  "+4 tests in `tests/test_concurrency.py`"). ❌
- **`## Verification split (this chat)` (`docs/HANDOFF.md:63-69`) cites the OLD numbers**:
  "189 passed, coverage 79.26%". ❌

The header, ledger rows, and standup addendum were updated; the two narrative body sections were
not. The accurate record of this chat's work lives only in the standup addendum
(`docs/memory/standups/2026-06-10-lut-report.md:108-120`). `/closeout` should rewrite the whole
baton, not patch its header. **Next baton rewrite must replace both stale sections.**

## Definition of done

- Tests green ✅ (197 passed, all four gates green, re-run by auditor)
- Audit clean → **CONCERNS** (protocol drift; no material code finding)
- **NEXT ACTION filled and scoped** ✅ — `docs/HANDOFF.md:29-47`: general `Idempotency-Key`
  header (replay original response; key stored in the same transaction as the effect) +
  one-shot-grant uniqueness (stipend per `(player_id, day)`, achievement per `(player_id, key)`),
  with explicit Scope / Risks / Off-limits lines. Present and well-scoped.

## Device-verifiable vs agent-verifiable

- **Agent proved:** diff == claims for all 4 code changes (file:line above); old 500/404-mapping
  failure modes confirmed against the base commit; 197/80.01%/lint/memory/migrations green;
  the 8 new tests pass by name; no web files touched; no ledger row silently dropped.
- **Owner must confirm by hand:** (1) the merge-before-audit ordering is acceptable as a one-off
  or the protocol order is restored; (2) the sensor bounds (−20..80 °C, CO₂ ≤5000, light ≤10000,
  pH 0–14) and the `max_actions ≤ 100` hard-reject (vs the old clamp) match design intent;
  (3) the next `/closeout` rewrites the stale baton body sections; (4) re-add the mint-endpoint
  HTTP test gap to RISK #8's remaining list.

## Verdict

**CONCERNS** — All material code/test claims verified with evidence and every gate re-ran green
(197 passed, 80.01% ≥ 79%), but the baton's "What THIS chat did" + "Verification split" sections
still describe the previous chat (protocol drift), the PR was merged before audit, one
scope-adjacent settings commit rode along, and RISK #8's mint-endpoint test gap quietly left the
ledger text.
